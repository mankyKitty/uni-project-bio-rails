<table id="genes-table" class="table table-hover table-condensed">
  <thead>
    <tr>
      <th ng-repeat="column in regpreciseRequestGeneByRegulonHeaders">
        {{column}}
      </th>
    </tr>
  </thead>
  <tbody>
    <tr ng-repeat="row in regpreciseRequestGeneByRegulon">
      <td ng-repeat="(key, element) in row">
        {{element}}
      </td>
    </tr>
  </tbody>
</table>
