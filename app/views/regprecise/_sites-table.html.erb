<table id="sites-table" class="table table-hover table-condensed">
  <thead>
    <tr>
      <th ng-repeat="column in regpreciseRequestSiteByRegulonHeaders">
        {{column}}
      </th>
    </tr>
  </thead>
  <tbody>
    <tr ng-repeat="row in regpreciseRequestSiteByRegulon">
      <td ng-repeat="(key, element) in row">
        {{element}}
      </td>
    </tr>
  </tbody>
</table>
